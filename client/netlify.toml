[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "18"

# SPA Redirect - This is critical for React Router
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
  force = false

# Handle API calls (optional, if you want to proxy)
# [[redirects]]
#   from = "/api/*"
#   to = "https://mydailytask.vercel.app/api/:splat"
#   status = 200
#   force = true
